import { authenticatorCoreDomain } from './authenticator-core-domain';

describe('authenticatorCoreDomain', () => {
  it('should work', () => {
    expect(authenticatorCoreDomain()).toEqual('authenticator-core-domain');
  });
});
