import { authenticatorBackendApplicationUseCases } from './authenticator-backend-application-use-cases';

describe('authenticatorBackendApplicationUseCases', () => {
  it('should work', () => {
    expect(authenticatorBackendApplicationUseCases()).toEqual(
      'authenticator-backend-application-use-cases'
    );
  });
});
